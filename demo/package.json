{
    "dependencies": {
        "ketab-online-sdk": "1.3.1",
        "preact": "^10.28.1"
    },
    "devDependencies": {
        "@preact/preset-vite": "^2.10.2",
        "@types/node": "^25.0.3",
        "typescript": "~5.9.3",
        "vite": "^7.3.0"
    },
    "name": "demo",
    "private": true,
    "scripts": {
        "build": "tsc -b && vite build",
        "deploy": "npm run build && surge dist ketabonline.surge.sh",
        "dev": "vite",
        "preview": "vite preview"
    },
    "type": "module",
    "version": "1.0.0"
}
